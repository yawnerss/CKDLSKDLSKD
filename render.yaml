services:
  - type: web
    name: cloudflare-solver-api
    runtime: image          # <-- important: use Docker runtime
    repo: https://github.com/yourusername/your-repo
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
