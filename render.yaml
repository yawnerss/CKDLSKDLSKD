services:
  - type: web
    name: cloudflare-solver-api
    runtime: python
    runtimeVersion: '3.11'
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn cf_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
